#include "main.h"
#include <stdio.h>

/*
 * main - checks for uppercase characters
 *
 * Return: Always 0.
 */
int main(void)
{
	char c = 'A'
	{
		if (c <= 'Z')
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
	
	return (0);
}
